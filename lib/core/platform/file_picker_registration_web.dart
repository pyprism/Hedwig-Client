// file_picker >=11 declares its web plugin (FilePickerWeb) in pubspec, so the
// Flutter web plugin registrant wires it up automatically. No manual
// registration is required here.
void registerFilePicker() {}
